query EpisodeList($page:Int){
    episodes(page:$page)
    {
        info
        {
            next
        }
        results
        {
            id
            name
            air_date
            created
        }
    }
}